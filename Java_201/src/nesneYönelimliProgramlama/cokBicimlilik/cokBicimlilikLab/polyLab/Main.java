package nesneYönelimliProgramlama.cokBicimlilik.cokBicimlilikLab.polyLab;

import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
		
		
		/*
		DbConnection connection=new DbConnection();
		connection.oppenConnection();
		connection.executeQuery("SELECT * FROM student");
		connection.closeConnection();
		
		MySqlConnection mySqlConnection=new MySqlConnection();
		mySqlConnection.oppenConnection();
		mySqlConnection.executeQuery("SELECT * FROM student");
		mySqlConnection.closeConnection();
		
		MsSqlConnection msSqlConnection=new MsSqlConnection();
		msSqlConnection.oppenConnection();
		msSqlConnection.executeQuery("SELECT * FROM student");
		msSqlConnection.closeConnection();
		DbConnection connection=new OracleDbConnection();
		connection.oppenConnection();
		connection.executeQuery("SELECT * FROM student");
		connection.closeConnection();
		*/
		/*
		Scanner scanner = new Scanner(System.in);
		System.out.print("Data base tipini seçin : [1 (MsSQL),2(MySQL),3(Oracle)]:");
		int dbType=scanner.nextInt();
		
		DbConnection connection=null;
		switch (dbType) {
		case 1:
			connection=new MsSqlConnection();
			break;
		case 2:
			connection=new MySqlConnection();
			break;
		case 3:
			connection=new OracleDbConnection();
			break;

		default:
			System.out.println("YANLIŞ veri tabanı seçimi");
			break;
		}
		if (connection != null) {
			connection.oppenConnection();
			connection.executeQuery("SELECT * FROM student");
			connection.closeConnection();
			
		}
		*/
		
		
		
		

	}

}
